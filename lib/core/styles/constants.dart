import 'package:flutter/material.dart';

class AppLength {
  static const double none = 0;
  static const double four = 4.0;
  static const double tiny = 8.0;
  static const double xxs = 10.0;
  static const double details = 11.0;
  static const double xs = 12.0;
  static const double sm = 14.0;
  static const double body = 16.0;
  static const double lg = 18.0;
  static const double xl = 20.0;
  static const double xxl = 24.0;
  static const double xxxl = 30.0;
}

class AppColors {
  // Primary colors
  static const Color primary = Color.fromRGBO(0, 0, 0, 1);
  static const Color secondary = Color.fromRGBO(176, 157, 53, 1);
  static const Color white = Color(0xFFFFFFFF);
  static const Color appBg = Color.fromRGBO(236, 236, 236, 1);
  static const Color bgCom = Color.fromRGBO(216, 216, 216, 1);
  static const Color blueGrey = Color.fromRGBO(115, 133, 172, 1);

  // Text colors
  static const Color textPrimary = Color.fromRGBO(41, 40, 76, 1);
  static const Color textSecondary = Color.fromRGBO(133, 132, 153, 1);
  static const Color textDarkGrey = Color.fromRGBO(64, 64, 64, 1);
  static const Color textLinkColor = Color.fromRGBO(80, 112, 182, 1);

  // Border colors
  static const Color borderColor = Color.fromRGBO(245, 245, 245, 1);

  // Background colors
  static const Color backgroundLight = Color.fromRGBO(240, 242, 244, 1);
  static const Color backgroundDark = Color(0xFF212121);
  static const Color secondaryLight = Color.fromRGBO(237, 241, 247, 1);
  static const Color dividerColor = Color.fromRGBO(211, 217, 223, 1);
  static const Color bgLight = Color.fromRGBO(240, 242, 244, 1);

  // Button colors
  static const Color buttonDisabled = Color.fromRGBO(234, 237, 240, 1);

  static const Color grey2 = Color.fromRGBO(175, 174, 174, 1);
  static const Color lightGrey = Color.fromRGBO(216, 216, 216, 1);
  static const Color darkGrey = Color.fromRGBO(122, 122, 122, 1);
}
