import 'package:flutter/material.dart';

class CoworkingPage extends StatelessWidget {
  const CoworkingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.white,
        child: const Center(
          child: Text('Coworking Page'),
        ),
      ),
    );
  }
}
